import { StyleSheet } from "react-native";

const global = {
  container: { 
      padding: 10,
      backgroundColor: 'white',
      flex: 1
  },
  title: {
      fontSize: 18,
      color: 'white'
  }
};

export const globalStyle = StyleSheet.create(global);
